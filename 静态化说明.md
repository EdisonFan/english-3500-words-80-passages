# 高考英语 3500 词静态化项目说明

## 项目目标

将 `spapro/` 目录下的动态 SPA 应用（依赖 Python 后端 + 有道词典 API）静态化为纯静态 HTML 站点，实现：
- 双击 HTML 即可打开，无需后端
- 保留原应用的全部交互功能（单词高亮点击弹窗、中文释义/译文开关、上下篇导航）
- 单词词典释义预缓存到本地 JSON 文件，无需联网查询

## 目录结构

```
/workspace/
├── spapro/                      # 原动态项目（数据源）
│   ├── data/
│   │   ├── p01.json ~ p80.json  # 80 篇文章数据（含正文/译文/词表）
│   │   └── cache/               # 原动态版词典缓存（参考）
│   ├── app.js                   # 原 SPA 渲染逻辑（参考）
│   ├── server.py                # 原后端（含词典 API 解析逻辑，参考）
│   ├── index.html / style.css   # 原前端骨架
│   └── 数据格式说明.md
├── HTML/                        # 静态化产出（本说明的目标）
│   ├── index.html               # 静态首页（16 单元 80 篇链接）
│   ├── passage-01.html          # 静态第一篇（含内联数据 + 交互脚本）
│   ├── passage-NN.html          # 其余 79 篇（待生成）
│   ├── dict/                    # 单词释义预缓存目录
│   │   ├── soil.json            # 每个单词一个 JSON 文件
│   │   ├── students.json
│   │   └── ... (按单词数计)
│   ├── style.css                # 复制自 spapro
│   ├── passage-static.js        # 静态版交互脚本（开关 + 弹窗）
│   ├── gen_index.js             # 生成索引页脚本
│   ├── gen_passage.js           # 生成单篇文章脚本
│   └── cache_words.js           # 拉取单词缓存脚本
└── 静态化说明.md                # 本文档
```

## 数据源格式

`spapro/data/pNN.json` 每篇文章数据结构：

```json
{
  "id": 1,
  "stats": { "words": 273, "core": 64 },
  "paragraphs": [
    {
      "num": "01",
      "en": "Turning {soil}, {pull} {weed} , and harvesting {cabbage} ...",
      "cn": "对于 <u> 初中 </u> 和高中的 <u> 孩子 </u> 们来说..."
    }
  ],
  "vocab": [
    { "word": "soil", "type": "outline|core", "ctx": "土壤" }
  ],
  "audio": "https://...mp3",
  "duration": "04:25"
}
```

**关键约定**：
- 正文 `en` 中 `{word}` 标记的词会高亮显示（带颜色），对应 `vocab` 列表中的词
- 中文译文 `cn` 中 `<u>词</u>` 标记重点词
- `vocab.type`：`outline`=大纲词（橙色），`core`=核心词（蓝色）

## 静态化步骤

### 第 1 步：准备工作目录

```bash
mkdir -p /workspace/HTML
cp /workspace/spapro/style.css /workspace/HTML/style.css
```

### 第 2 步：生成静态索引页

运行 `gen_index.js`（已写好，路径 `/workspace/HTML/gen_index.js`）：

```bash
cd /workspace/HTML
node gen_index.js
```

脚本会读取内置的 16 个单元配置，生成 `index.html`，包含 80 个文章链接（指向 `passage-NN.html`）。

### 第 3 步：生成单篇静态文章

运行 `gen_passage.js`（已写好，路径 `/workspace/HTML/gen_passage.js`）：

```bash
cd /workspace/HTML
node gen_passage.js   # 默认生成 passage-01.html
```

**如需生成其他篇**：修改 `gen_passage.js` 中的 `var passageId = 1`，或改为从命令行参数读取（参考 `cache_words.js` 的 `process.argv[2]` 写法）。

**脚本做的事**：
1. 读取 `spapro/data/pNN.json`
2. 复用 `spapro/app.js` 的渲染逻辑（`highlightWords`、`tokenizeAndWrap`、`findVocab`、`renderVocabCard` 等）
3. 生成顶栏（含开关按钮、上下篇导航）、正文段落、词表、页脚
4. 内联 vocab 数据到 `window.__VOCAB__`
5. 引用 `passage-static.js` 提供交互

### 第 4 步：预缓存单词释义（关键）

运行 `cache_words.js`（已写好，路径 `/workspace/HTML/cache_words.js`）：

```bash
cd /workspace/HTML
node cache_words.js 1      # 缓存第 1 篇的所有单词
node cache_words.js 2      # 缓存第 2 篇
# ... 或写循环批量缓存
```

**脚本做的事**：
1. 读取 `spapro/data/pNN.json`
2. 提取两类单词并合并去重：
   - `vocab` 列表的高亮词（带颜色的）
   - 正文 `en` 中所有英文单词（包括非高亮的普通词，如 `the`/`is`/`students` 等）
3. 对每个单词调用有道词典 API：`https://dict.youdao.com/jsonapi?q=<word>`
4. 解析返回数据，生成统一结构的 JSON，保存到 `dict/<word>.json`
5. 已缓存的单词自动跳过（支持断点续传）

### 第 5 步：启动静态服务预览

```bash
cd /workspace/HTML
python3 -m http.server 8000
# 访问 http://localhost:8000/index.html
```

## 单词缓存 JSON 格式

每个 `dict/<word>.json` 文件结构：

```json
{
  "word": "soil",
  "found": true,
  "phonetic_uk": "sɔɪl",
  "phonetic_us": "sɔɪl",
  "audio_uk": "https://dict.youdao.com/dictvoice?audio=soil&type=1",
  "audio_us": "https://dict.youdao.com/dictvoice?audio=soil&type=2",
  "defs": [
    { "pos": "n.", "meaning": "泥土，土壤；..." },
    { "pos": "v.", "meaning": "弄脏，玷污" }
  ],
  "forms": [
    { "name": "复数", "value": "soils" },
    { "name": "过去式", "value": "soiled" }
  ],
  "examples": [
    { "en": "Farmers grow rice in the rich soil.", "zh": "农民们在肥沃的土地上种水稻。" }
  ],
  "synonyms": [
    { "pos": "n.", "meaning": "土地；土壤", "words": ["nation", "country", "ground"] }
  ]
}
```

## 关键技术点

### 1. 单词提取范围

**必须同时缓存两类单词**：
- `vocab` 列表的词（高亮带颜色，约 60 个/篇）
- 正文 `en` 中所有英文单词（灰色下划线，约 110 个/篇）

合并去重后，每篇约 170 个单词。

提取正文单词的正则：`/[A-Za-z][A-Za-z']*/g`，并去掉首尾撇号。

### 2. 有道 API 解析逻辑

复制自 `spapro/server.py` 的 `_fetch_youdao` 和 `_parse_result`，解析 4 个数据源：
1. `ec`（英汉词典）→ 音标、释义、变形
2. `simple`（兜底）→ 释义
3. `blng_sents_part`（双语例句）
4. `syno`（同义词）

词性分割正则：`/^((?:n|v|vi|vt|aux|adj|adv|prep|conj|pron|num|art|int|abbr|det)\.)\s*(.*)/`

### 3. 所有格词回退

所有格词（如 `program's`、`jaramillo's`）有道 API 查不到，会缓存为 `found:false`。
前端 `loadDict()` 已处理回退：查不到时自动去掉 `'s` 查原形（`program`），复用原形的缓存。

### 4. 环境代理问题

本环境有 HTTP 代理：`http://127.0.0.1:18080`
- `curl` 自动使用代理 ✅
- Node.js 的 `https.get` **不自动使用代理** ❌（会 ETIMEDOUT）

**解决方案**：`cache_words.js` 中用 `execSync('curl ...')` 拉取数据，而非 Node 原生 https 模块。

如在其他环境运行，可改回 Node https 模块（若该环境无代理或 Node 配了代理）。

### 5. 静态版交互脚本

`passage-static.js` 提供：
- 中文释义开关（`.gloss-toggle`，localStorage 持久化 key: `spa_gloss`）
- 中文译文开关（`.trans-toggle`，localStorage 持久化 key: `spa_trans`）
- 单词点击弹窗（从 `dict/<word>.json` 加载，含音标、发音按钮、释义、变形、例句、同义词）
- 弹窗关闭（点击关闭按钮 / 点击遮罩）

## 批量处理建议

### 批量缓存 80 篇单词

```bash
cd /workspace/HTML
for i in $(seq 1 80); do
  num=$(printf "%02d" $i)
  echo "=== Passage $num ==="
  node cache_words.js $i
done
```

注意每篇约 170 词，80 篇约 13600 词，去重后估计 5000-8000 词，按 300ms/词间隔约需 30-40 分钟。建议分批跑（可中断，已缓存自动跳过）。

### 批量生成 80 篇静态文章

修改 `gen_passage.js` 支持命令行参数：

```js
var passageId = parseInt(process.argv[2] || '1', 10);
```

然后批量生成：

```bash
for i in $(seq 1 80); do node gen_passage.js $i; done
```

## 验证清单

每完成一篇后检查：
- [ ] `passage-NN.html` 文件生成成功
- [ ] `dict/` 目录下该篇所有单词都有对应 JSON
- [ ] 浏览器打开 `passage-NN.html`，正文显示正常
- [ ] 点击高亮词（蓝色/橙色）弹出词典弹窗
- [ ] 点击普通词（灰色下划线）弹出词典弹窗
- [ ] 弹窗含音标、释义、例句
- [ ] "中文释义"开关可切换单词下方注释显隐
- [ ] "中文译文"开关可切换段落下方译文显隐
- [ ] 上一篇/下一篇导航链接正确

## 常见问题

**Q: 为什么有些词显示"该词释义暂未预缓存"？**
A: 该词未在 `dict/` 目录缓存。运行 `node cache_words.js <篇号>` 补充缓存。或前端已自动回退查原形（所有格词）。

**Q: 缓存文件为什么有 `found:false` 的？**
A: 有道 API 对该词形查不到（如所有格 `program's`、专有名词变形）。前端会自动回退查原形。

**Q: 下一篇链接 404？**
A: 该篇静态 HTML 未生成。运行 `node gen_passage.js <篇号>` 生成。

**Q: 想更新某个词的释义？**
A: 删除 `dict/<word>.json`，重新运行 `node cache_words.js <篇号>`。

---

**参考文件**：
- 原动态版渲染逻辑：`spapro/app.js`
- 原后端词典解析：`spapro/server.py`
- 数据格式说明：`spapro/数据格式说明.md`
- 静态化脚本：`HTML/gen_index.js`、`HTML/gen_passage.js`、`HTML/cache_words.js`
- 静态交互脚本：`HTML/passage-static.js`
